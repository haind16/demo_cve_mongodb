# mongodb-cve-lab

This repo contains multiple MongoDB CVE labs:

- `cve-2019-2386/` — Authorization Session Conflation demo (vulnerable vs fixed)
- `cve-20xx-YYYY/` — (describe your second lab)

## Quick start
See each lab's README for steps (docker compose up, setup scripts, PoC, A/B compare).
